import * as React from 'react';
import * as s from './accordion.css'

interface Props {

}

export default class Accordion extends React.Component<Props, null> {
  render() {
    return (
      <div className={s.accordion}>
      </div>
    )
  }
}